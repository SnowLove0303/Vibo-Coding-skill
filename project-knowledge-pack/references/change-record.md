# 资料变更单

变更编号：

项目与模块：

责任线程与日期：

事实变化：

证据位置：

影响分区：协作与资料索引 / 需求与质量 / 技术与运行

需要同步的 AIstudy Ref 或本地资料：

当前核验状态：已验证 / 未验证 / 外部阻断

风险、冲突与回滚：

下一线程启动时必须读取：
用户级交互影响：记录本次变化是否影响普通用户主路径、真实入口、可选操作、状态、进度、结果、失败原因、下一步、回退路径、旧版功能保留情况，以及是否存在仅调试入口、内部命令、`moduleId/action`、`taskId`、原始 JSON 或底层能力可用但用户功能未完成的情况。

执行记录与反馈：涉及开发、修复、优化、重构、UI、自动化、RPA、API、数据库、打包、发布或整改时，记录技术执行线程的任务理解、实施步骤、修改文件、关键命令、测试/自测记录、失败与重试记录、问题反馈、未解决风险、人工介入项、回滚点、逐项功能实现汇报和既有功能影响说明；审计线程必须读取这些记录后再判定。

一次性需求与整改清单：涉及需求发出、审计反馈或整改派发时，记录当前目标的完整用户视角功能清单、旧功能基线、必须实现项、禁止破坏项、已发现不足、疑似风险、真实入口、数据/权限/配置影响、回归范围、人工介入项、通过标准、失败标准、新增证据和避免继续碎片化返工的处理方式。
最新版入口与 PowerShell 指令：涉及开发、修复、优化、打包、发布或审计交接时，记录用户打开最新版的真实入口、PowerShell 启动/验证/停止指令、工作目录、最新包或服务路径、快捷方式指向、旧进程/旧包/旧入口隔离情况、预期可见版本或变化、异常时的最小处理方式。
用户级实时性与状态一致性：涉及面向用户的应用、服务、调度层、模块或自动化功能时，记录用户真实入口、状态事实源、数据刷新/同步机制、耗时操作进度反馈、任务结束与异常发现方式、页面/列表/汇总/任务记录一致性要求、历史/缓存/快照/推断结果的时间来源和有效性标识、失败/等待/阻塞/超时/资源占用处理路径、并发影响和最少操作路径。
指令化操作与模块独立边界：记录功能的 PowerShell、CLI、API、MCP、脚本、HTTP 探针、程序内置指令、状态查询或日志核验路径，以及预期输出、失败判断和回归方式。涉及多模块时，记录模块边界、独立性要求、仓库/发布/回滚/审计边界、分别修改验证提交/推送计划、跨模块对接关系和独立证据路径。
完整功能需求与分段计划：记录完整功能需求、最终业务结果、完整功能链路、数据流、状态流、异常流、依赖关系、验收标准、回归范围、分段执行计划、阶段目标、完成标准、当前进度、剩余缺口、模块责任、风险变化和验收证据，避免最小实现长期化。
指令化操作与模块独立边界：记录功能的 PowerShell、CLI、API、MCP、脚本、HTTP 探针、程序内置指令、状态查询或日志核验路径，以及预期输出、失败判断和回归方式。涉及多模块时，记录模块边界、独立性要求、仓库/发布/回滚/审计边界、分别修改验证提交/推送计划、跨模块对接关系和独立证据路径。
完整功能需求与分段计划：记录完整功能需求、最终业务结果、完整功能链路、数据流、状态流、异常流、依赖关系、验收标准、回归范围、分段执行计划、阶段目标、完成标准、当前进度、剩余缺口、模块责任、风险变化和验收证据，避免最小实现长期化。
